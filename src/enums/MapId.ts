/** Map identifiers — OpenWorld gameplay uses `MEXICO`, not `OPENWORLD`. */
export enum MapId {
  /** The Mexico GLB map. Used by OpenWorld mode, FFA, and Team modes. */
  MEXICO = 'mexico',
  IMSS = 'imss',
  CITY = 'city',
  METRO = 'metro',
}
