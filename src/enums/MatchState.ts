/**
 * @license
 * SPDX-License-Identifier: Apache-2.0
 */

export enum MatchState {
  WAITING = 'waiting',
  STARTING = 'starting',
  IN_PROGRESS = 'in_progress',
  OVERTIME = 'overtime',
  FINISHED = 'finished',
}
